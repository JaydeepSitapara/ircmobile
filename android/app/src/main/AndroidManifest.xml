<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        SYSTEM_ALERT_WINDOW — required for Phase 3 floating overlay.
        Declared here so the app can check/request it in Settings.
        The overlay itself is not implemented until Phase 3.
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- ── Main Flutter activity ──────────────────────────────────────── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Accessibility Service ─────────────────────────────────────── -->
        <!--
            BIND_ACCESSIBILITY_SERVICE is automatically required for any
            service declared with this permission attribute.

            android:exported="false"  — this service must NOT be exported;
            only the system Accessibility Manager should bind to it.

            The description string shown in Settings → Accessibility is
            defined in res/values/strings.xml and must honestly explain
            the service's purpose (required by Google Play policy).
        -->
        <service
            android:name=".ReelsAccessibilityService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Required by the Flutter tool for plugin registration. -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- ── Package visibility ──────────────────────────────────────────────── -->
    <!--
        Android 11+ requires explicit package-visibility declarations.
        We query Instagram's package to check if it is installed.
    -->
    <queries>
        <!-- Allow querying whether Instagram is installed. -->
        <package android:name="com.instagram.android" />

        <!-- Required by the Flutter engine text-processing plugin. -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
